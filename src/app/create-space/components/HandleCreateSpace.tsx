import { Loader2 } from "lucide-react"

const HandleCreateSpace = () => {
    return (
        <div>
            <Loader2 size={60} className="fcb mt-10 w-full text-primary animate-spin" />
        </div>
    )
}

export default HandleCreateSpace
